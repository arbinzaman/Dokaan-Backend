generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
}

generator erd {
  provider = "prisma-erd-generator"
  output   = "../output/erd.svg"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
